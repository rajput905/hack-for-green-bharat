# ── Lightweight test-only dependencies ─────────────────────────────────────────
# Used in CI to avoid installing heavy ML deps (chromadb, onnxruntime, etc.)
# that add 10+ minutes to the install step.

fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1
sqlalchemy==2.0.29
aiosqlite==0.20.0
httpx==0.27.0
python-dotenv==1.0.1
pytest==8.1.1
pytest-asyncio==0.23.6
